#Selection Sort
if nums == None or len(nums) == 0:
    return        
for i in range(len(nums) - 1):
    for j in range(len(nums)):
        if nums[j] == min(nums[i:]):
            nums[i], nums[j] = nums[j], nums[i]
            break
